// reverse_bits char octet
 


// add_prime_sum



//sort_list



//ft_list_remove_if
tlist curr = begin 
tlist prev = NULL

while curr 
	if cmp 
		if head,  prev == null 
			begin = curr>next 
		else not head 
			prev>next = curr>next 
		free curr
		curr = prev 
	prev = curr 
	curr = curr>next 