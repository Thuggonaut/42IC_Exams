// reverse_bits char octet
 


// add_prime_sum



//sort_list
tlist tmp 
tlist head = lst 

while lst>next 
	if cmp 
		swap 
		tmp = lst>dat 
		lst>data = lst>data>next 
		lst>data>next> = tmp 
		lst = head 
	else
		lst = lst>next
	lst = head
	


//ft_list_remove_if
tlist curr = begin 
tlist prev = null 

while curr 
	if cmp 
		if pre = null, head 
			begin = curr>next 
		else not head 
			prev>next - curr>next 
		free curr 
		curr = prev 
	prev = curr 
	curr = curr>nect